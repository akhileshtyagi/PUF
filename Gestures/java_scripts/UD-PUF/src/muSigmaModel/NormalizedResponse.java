package muSigmaModel;

import java.util.List;

import data.ChallengeResponse;

/**
 * Takes a ChallengeResponse object in its constructor and creates the mu-sigma
 * model from this.
 * 
 * @author element
 *
 */
public class NormalizedResponse {
    List<List<Double>> sigmaModel;

    /**
     * Takes a challenge response object. Uses this object to construct the
     * normalized mu-sigma model.
     * 
     * @param challengeResponse
     */
    public NormalizedResponse(ChallengeResponse challengeResponse) {
	// TODO
    }
}
